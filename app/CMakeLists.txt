if(CONFIG_CUSTOM_APP)
  nuttx_add_application(
    NAME
    ${CONFIG_CUSTOM_APP_PROGNAME}
    PRIORITY
    ${CONFIG_CUSTOM_APP_PRIORITY}
    STACKSIZE
    ${CONFIG_CUSTOM_APP_STACKSIZE}
    MODULE
    ${CONFIG_CUSTOM_APP}
    SRCS
    custom_app_main.c)
endif()
